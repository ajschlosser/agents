Be concise and sparse with tokens for language/reasoning.

If writing code, always test your changes by compiling, running, etc.

If a git repository is present, always run `git status`. Commit changes as needed with a helpful message.

If possible, push the changes to the git remote.
